<div class="card">
    <div class="card-header">
        <h1>New Friend</h1>
    </div>
    <div class="card-body">
        <%= render 'form', friend: @friend %>
        <%= link_to 'Back', friends_path, class:"btn btn-secondary" %>
    </div>
</div>

